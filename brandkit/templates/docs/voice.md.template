# Voice — [PROJECT_NAME]

<!-- Stub created by the brandkit skill (init-docs). Fill each "Pending
     definition" as copy decisions land — flag, don't invent. UI language:
     [UI_LANG]. -->

## Address and register

Pending definition — how the product addresses the user (formal/informal),
person, tone.

## Voice qualities

Pending definition — 3-5 adjectives with a one-line "means / never means"
each.

## Domain vocabulary

Pending definition — table: term · use · never say. Terms here are
**substance**: a specimen may reword prose, never a term.

## Errors

Pending definition — the error pattern (what failed · what survived · way
out), with one canonical example.

## Empty states

Pending definition — the cases (never existed / not here / no matches) and
their pattern.

## Destructive confirmation

Pending definition — how destructive actions ask (what is lost, named; the
verb on the button).

## Conventions

- Every string the BrandKit's Voice tab shows is copied from here verbatim —
  wording licence exists, rule licence does not.
