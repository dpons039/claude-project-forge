# Planning

Only pending tasks `[ ]`. Never `[x]`. Entries: 1–2 lines + pointer — no
inline diagnoses or narrative (link a doc, proposal, or `_archive/` file).
Hard cap: **100 lines, current phase only** — the full phase plan and deferred
future scope live in `roadmap.md`, read on demand.
Completed group tasks → ~~strikethrough~~, 1 line (don't delete until the whole group is struck).
On completing standalone task: delete the line + summary to `changelog.md` in the same commit.
On completing entire group: delete the block + summary to `changelog.md`.

---
