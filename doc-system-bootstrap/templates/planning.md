# Planning

Only pending tasks `[ ]`. Never `[x]`.
Completed group tasks → ~~strikethrough~~ (don't delete until entire group is struck through).
On completing standalone task: delete the line + summary to `changelog.md` in the same commit.
On completing entire group: delete the block + summary to `changelog.md`.

---
