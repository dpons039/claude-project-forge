# {Feature} Implementation Plan

> For agentic workers: REQUIRED SUB-SKILL — superpowers:subagent-driven-development
> (fan-out only) or superpowers:executing-plans (dependent chain, the default here;
> see .claude/rules/code-quality.md § When executing a plan). Steps use `- [ ]`.
>
> This file is the DURABLE, git-tracked step list (complex flow only). It is NOT
> session.md: no live state, no run status, no commit SHAs, no dated follow-ups —
> that lives in session.md and is pruned there. A plan.md that accumulates `[x] DONE
> (sha)` has drifted into session.md's job.

**Spec:** ./proposal.md
<!-- The plan argues FROM the proposal of this {slug}; executors read both. The
     proposal is the verified spec; this plan is only the how. Do not restate the
     spec here, and do not put spec/premises in the plan. -->

**Gate:** proposal Status must be `verified` before implementing.
<!-- LAW8: never implement from `approved` — only `verified` (premises re-checked
     against the code as it stands now). If it is not verified, stop and verify. -->

## Global Constraints
<!-- The rules EVERY task obeys — one line each, exact values. Every task's steps
     implicitly include this block. Copy the spec's project-wide requirements here
     (version floors, naming/copy rules) plus the standing laws below. -->
- Language: everything written is [CODE_LANG] (LAW7); only chat replies are [RESPONSE_LANG].
- Branch: work on the [DEV_BRANCH] branch; never commit to [PROTECTED_BRANCH] (LAW6).
- Baseline: the suite is green before starting; any red after a task is yours to fix.
- Commits: the per-task `Commit` steps are SUGGESTED points, not automatic actions.
  Executing a commit always waits for the owner's word (Autonomy); a UI change waits
  for the owner's render approval first (LAW2). Do not `git commit` per task on your own.
- {any spec-specific constraint: a UI-kit-only rule, PK convention, DB casing, ...}

**Goal:** {one sentence — what this builds}
**Architecture:** {2-3 sentences — the approach}
**Tech Stack:** {key technologies for this change}

---

<!-- From here down is generated by superpowers:writing-plans, ADAPTED TO THIS STACK
     (use the project's real test runner and language, NOT the skill's Python/pytest
     examples):
       - File Structure / File map (Create / Modify / Test, one responsibility each)
       - Task N blocks (Files, Interfaces Consumes/Produces, bite-sized `- [ ]` steps)
       - Verification (end-to-end, runnable commands the project declares)
       - Self-Review (spec coverage, no placeholders, type consistency)
     Do not hand-write these from this template; invoke the skill so they match the
     current code. See docs/changes/README.md § Mandatory superpowers per phase. -->
