---
name: brandkit-manager
description: "Create, maintain, and audit a project's dev-only BrandKit page and its satellite docs (docs/BrandKit.md, docs/design.md, docs/voice.md, root PRODUCT.md). The page is an MDX-driven, config-parameterised visualizer: a fixed universal chrome (shipped as copy-paste files) plus, per tab, a .mdx (prose rule + a live demo showing the specimen and its copyable source) paired with a .examples.tsx (the stateful specimens the .mdx imports), all varying content declared in config.ts. FOUR OPERATIONS: (1) init-docs — create the doc satellites, works without a frontend. (2) init-page — scaffold the dev-only page (separate build entry, MDX chrome, config.ts, live tokens) once a frontend exists. (3) update — add/change/remove a block, wireframe, accent, tone or language while keeping page and docs coherent. (4) audit — check page vs real tokens/components/strings/docs for drift; findings report, no fixes applied. Trigger on: 'brandkit', 'brand kit', 'design system page', 'audit the brandkit', 'add a block to the brandkit', 'brandkit is stale', 'set up brand kit docs'. Use when a project needs a living copy-paste reference for its design system, or to check whether an existing one has drifted from the tokens, components and copy it claims to reflect."
---

# BrandKit Manager

Permanent per-project skill managing a dev-only design-system page and its
satellite docs. The page shows every design decision rendered next to the code
that produces it; the docs hold the reasoning. This skill keeps both coherent.

The page is an **MDX-driven, config-parameterised visualizer**: a fixed universal
chrome (nav, the dual live-preview-plus-code block, the on-this-page rail, the
global theme/accent/density/width controls) that no project edits, plus content
the project supplies — `config.ts` and, per tab, a `<id>.mdx` (prose rule + the
live `<Demo>`) paired with a `<id>.examples.tsx` (the stateful specimens the
`.mdx` imports — anything with hooks, i18n, or types, which cannot live inline in
MDX). A developer adds a tab by writing that `.mdx` (+ its `.examples.tsx` if a
specimen needs state) and one config line; changes a visual detail from the
config. The chrome is icon-library- and token-independent so it drops into any
project unchanged.

Not a design-authoring skill: it documents and verifies the design system, it
does not invent one. The "exactly ONE design skill per project" rule does not
apply to it.

## The satellites

| Artefact | Role |
|---|---|
| `frontend/brandkit.html` + `frontend/src/brandkit/` | The page — dev-only **by construction** (entry absent from build inputs) |
| `docs/BrandKit.md` | How the page works, how to add blocks — **owned by this skill** |
| `docs/design.md` | Tokens, hierarchy, themes — reasoning upstream of the page |
| `docs/voice.md` | Tone, vocabulary — source of every specimen string |
| `PRODUCT.md` (root) | Product context (impeccable v4); § Brand Commitments names mark/colour/type/a11y |

The page shows; it does not decide. When page and docs disagree, the doc is
right and the page is the bug.

## Operation Detection

1. Check what exists: `docs/BrandKit.md`? A page (`src/brandkit/` or a brandkit
   entry next to the app's index)?
2. Route:
   - Neither exists → **init-docs**; then offer **init-page** if a **web**
     frontend exists (a desktop GUI is not one — init-page needs a build with
     declarable entries).
   - Doc exists, no page → **init-page**.
   - Both exist and the user wants a change → **update**.
   - Both exist and the user wants a health check → **audit**.
   - Ambiguous trigger ("check the brandkit", "brandkit is stale") → **audit**
     (read-only, safest default).

## Operation 1 — init-docs

Works **without a frontend**. Creates `docs/BrandKit.md` from the template;
stubs `docs/design.md` / `docs/voice.md` only if missing; appends
§ Brand Commitments to an existing `PRODUCT.md` if that section is missing.
Never overwrites an existing file — an existing `BrandKit.md` is `update`
territory. `PRODUCT.md` itself is generated by `/impeccable teach`
(project-bootstrap), never by this skill.

Read `references/init-docs-guide.md`.

## Operation 2 — init-page

Requires a working frontend. Installs the page: **copy the universal chrome
from `templates/page/chrome/` unchanged**, wire the build (a separate entry
excluded from production by construction + the MDX plugin), fill `config.ts`
from its template, and write one `.mdx` per tab. The chrome is copy-paste-
identical across projects; a project varies only `config.ts`, the tabs, and
the mark component config points at.

The tab set is `templates/page/page-structure.md` (8 tabs: Brand · Foundations ·
Colour · Components · Formatting · Layouts · Voice · Accessibility). Skipping a
canonical tab or block requires a "skipped: <reason>" note in `BrandKit.md`.

Read `references/init-page-guide.md`.

## Operation 3 — update

Any change to the system — new block, new wireframe, token change, copy
change, new accent or theme, new UI language, removal — walks the
**cross-update table** so page, `BrandKit.md`, `design.md`, `voice.md` and
`PRODUCT.md` move together. Never stop at "the page is updated".

Read `references/update-guide.md`.

## Operation 4 — audit

Read-only. Runs 16 mechanical checks (gating, live tokens, block inventory,
string provenance, wireframe corrections, transcribed numbers, encapsulation,
canonical coverage, doc coherence, size caps) and produces a findings report
with cited evidence. Applies **no fixes** — fixes are a follow-up task the
report feeds.

Read `references/audit-checklist.md`.

## Templates

| Template | Installs to | Type |
|----------|------------|------|
| `templates/docs/BrandKit.md.template` | `docs/BrandKit.md` | Placeholders |
| `templates/docs/design.md.template` | `docs/design.md` (only if missing) | Stub, placeholders |
| `templates/docs/voice.md.template` | `docs/voice.md` (only if missing) | Stub, placeholders |
| `templates/docs/PRODUCT-brand-section.md.template` | appended to `PRODUCT.md` | Section stub, placeholders |
| `templates/page/chrome/` | copied unchanged into `src/brandkit/` | Real code — the universal chrome |
| `templates/page/config.ts.template` | `src/brandkit/config.ts` | Placeholders — the one per-project file |
| `templates/page/entry.html.template` | `frontend/brandkit.html` | Placeholders |
| `templates/page/tabs-example/` | a shape to copy per tab: `<id>.mdx` (+ `<id>.examples.tsx` when a specimen has state) + `index.ts` registration | Generic worked example |
| `templates/page/page-structure.md` | not copied — the tab outline `init-page` fills and `audit` checks against | Headings only |

**This skill ships the chrome as real files.** `templates/page/chrome/` is
copied in unchanged (the universal shell: nav, the dual live-preview-plus-code
`Demo`, the `Block`, the on-this-page rail, the controls, the `--bk-*` palette,
own inline icons). The per-project surface is `config.ts` (from its template),
the `.mdx` tabs, and the mark component config names. `.template` files carry
`[PLACEHOLDERS]`; the debugged techniques live as comments in the chrome and are
recapped in the guide.

## Assumptions

Vite-family build (explicit entry list → a sibling HTML entry is simply never
built), a React-family component model, and MDX via `@mdx-js/rollup` +
`@mdx-js/react` — the chrome assumes them. Different stack → read
`references/init-page-guide.md` § Adapting to a different stack for the principle
and adapt the chrome; the tab structure is stack-independent.

## References

- `references/init-docs-guide.md` — Operation 1 steps
- `references/init-page-guide.md` — Operation 2 steps + stack adaptation
- `references/update-guide.md` — Operation 3 cross-update table
- `references/audit-checklist.md` — Operation 4, the 16 checks
